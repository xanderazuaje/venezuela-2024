---
const { type, name, id, placeholder, required, ...props } = Astro.props;
---

<input
  {...props}
  type={type}
  name={name}
  id={id}
  placeholder={placeholder}
  class="w-full border-[#C1C1C1] border rounded-xl py-2 px-4 appearance-none"
  required={required}
/>
